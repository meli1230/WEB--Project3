<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Events</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>

<body class="container" style="margin-top: 50px;">

    <h1>Manage Events</h1>
    <p><a href="/">Home</a></p>
    <p><a href="/logout">Logout</a></p>

    <div class="container mt-4">
        <h2>Events</h2>

        <button class="btn btn-secondary" onclick="location.href='/events/sort?order=ASC'">Sort by Price (Low to High)</button>
        <button class="btn btn-secondary" onclick="location.href='/events/sort?order=DESC'">Sort by Price (High to Low)</button>
        <button onclick="location.href='/manage-events'" class="btn btn-primary">Reset Sorting</button>
        <br/>
        <br/>

        <table class="table table-striped table-bordered text-center">
            <thead class="table-dark">
            <tr>
                <th>Title</th>
                <th>Speaker</th>
                <th>Price (€)</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            <% events.forEach(event => { %>
                <tr>
                    <td><b><%= event.title %></b></td>
                    <td><b><%= event.speaker %></b></td>
                    <td><b><%= event.price %></b></td>
                    <td>
                        <a href="/edit-event/<%= event.id %>" class="btn btn-primary btn-sm">Edit</a>
                        <a href="/delete-event/<%= event.id %>" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    </div>


    <p><a href="/add-event" class="btn btn-success mt-3">Add Event</a></p>

</body>

</html>